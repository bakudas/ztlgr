// Agents for ztlgr
// Will be implemented with multiagent framework

pub struct NoteAgent;
pub struct LinkAgent;
pub struct SearchAgent;
pub struct GraphAgent;

// Placeholder implementations
// These will use the multiagent framework from the fleet project
